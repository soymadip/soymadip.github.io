# soymadip.github.io

Content and configuration for a personal website built with the Portosaurus.

- `config.js` — site settings and metadata
- `blog/` — posts and updates
- `notes/` — knowledge pages
- `src/` — static assets

Local build: `mise build`

Build pulls the upstream compiler from `.github/config.json` and outputs the site into `build/`.
