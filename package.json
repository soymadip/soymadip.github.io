{
  "name": "soymadip.gitlab.io",
  "private": true,
  "scripts": {
    "build": "bun static/compile.js",
    "dev": "bun run build && bun x live-server build --port=5677"
  },
  "dependencies": {
    "portosaurus": "^1.18.16",
    "live-server": "latest"
  }
}
