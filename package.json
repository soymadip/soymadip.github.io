{
  "name": "soymadip.github.io",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "$npm_execpath install && porto dev",
    "start": "$npm_execpath install && porto start",
    "build": "$npm_execpath install && porto build",
    "serve": "porto serve"
  },
  "dependencies": {
    "@portosaur/cli": "^0.19.0",
    "@portosaur/theme": "^0.19.0"
  }
}
