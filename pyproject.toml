[project]
name = "soymadip-github-io"
version = "0.0.0"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "fastapi[standard]>=0.141.1",
    "psutil>=7.2.2",
    "pydantic[email]>=2.13.4",
    "watchdog>=6.0.0",
]
