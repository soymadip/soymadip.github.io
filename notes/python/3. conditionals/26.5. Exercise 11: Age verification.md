---
id: 26-5-exercise-11-age-verification
title: Exercise 11: Age verification
sidebar_label: Exercise 11: Age verification
sidebar_position: 26.5
description: Convert string input to a number and use a ternary conditional for access control.
source_filename: "26.5. age verifaction.py"
---

# Age verification

What to remember

- `input()` results are strings; convert them before numeric checks.
- Use `int()` to parse numeric text.
- Use a ternary operator for compact conditional returns.

## Ternary Conditional Example

```python
def verify_age(age_str: str) -> str:
    age = int(age_str)
    return "Access Granted" if age >= 18 else "Access Denied"
```

```python
print(verify_age("18"))
```

## Why This Pattern Works

- `int(age_str)` converts the text to a number.
- The ternary expression returns one of two strings in a single line.
- It is ideal for simple binary choices like access control.

## Example Output

```text
Access Granted
```

## What to Watch For

:::warning
This function assumes valid numeric input. In a real app, validate the text before calling `int()`.
:::

:::tip
Wrap `int()` in `try`/`except ValueError` if user input may be malformed.
:::
