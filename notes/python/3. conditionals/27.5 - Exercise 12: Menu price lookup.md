---
id: 27-5-exercise-12-menu-price-lookup
title: "Exercise 12: Menu price lookup"
sidebar_label: "Exercise 12: Menu price lookup"
sidebar_position: 27.5
description: "Match-case exercise for menu price lookup with case-insensitive input."
source_filename: "27.5 - Menu price lookup.py"
---

# Ex: 12 - Menu price lookup

## Problem

You’re creating a menu price lookup system for a digital food ordering app using the `match` / `case` statement.

- Define `get_item_price(item)` to return a price message for valid menu items.
- Make the item check case-insensitive.
- Return a default message when the item is not available.

## Rules

- Define `get_item_price(item: str) -> str`.
- Normalize the input using `item.lower().strip()`.
- Use `match` / `case` for the menu branches.
- Return the exact text for each menu item.
- Use `case _` for unavailable items.
- Only view the solution after trying your best.

```text
Price: 15 bucks
Item not available
```

## Solution

Only view the solution after trying your best.

<details>
<summary>Show solution</summary>

```python
def get_item_price(item: str) -> str:
    match item.lower().strip():
        case "pizza":
            return "Price: 30 bucks"
        case "burger":
            return "Price: 15 bucks"
        case "pasta":
            return "Price: 20 bucks"
        case "salad":
            return "Price: 10 bucks"
        case _:
            return "Item not available"
```

</details>

## Why exact output matters

The function returns strings that are often checked by automated tests, so the return values must match exactly.
